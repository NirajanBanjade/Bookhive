// Main Profile component (smart controller)
// Import this to use the automatic view/edit switching
export { default } from "./Profile";

// Individual components for direct use when needed
// These can be imported directly if you need specific functionality
export { default as ProfileView } from "./ProfileView";
export { default as ProfileForm } from "./ProfileForm";
