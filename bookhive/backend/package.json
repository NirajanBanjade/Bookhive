{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
"scripts": {
    "server": "node server.js",
    "server:watch": "nodemon server.js",
    "test": "cross-env NODE_ENV=test mocha TESTS --timeout 10000",
    "test:registration": "cross-env NODE_ENV=test mocha TESTS/registration.test.js --timeout 10000",
    "test:login": "cross-env NODE_ENV=test mocha TESTS/login.test.js --timeout 10000",
    "test:password-reset": "cross-env NODE_ENV=test mocha TESTS/passwordReset.test.js --timeout 10000",
    "test:auth": "cross-env NODE_ENV=test mocha TESTS/registration.test.js TESTS/login.test.js TESTS/passwordReset.test.js --timeout 10000",
    "test:notifications": "cross-env NODE_ENV=test mocha TESTS/notification.routes.test.js --timeout 10000",

    "report:auth": "cross-env NODE_ENV=test mocha TESTS/registration.test.js TESTS/login.test.js TESTS/passwordReset.test.js --reporter mochawesome --reporter-options reportDir=TESTS/nirajanbanjade-report,reportFilename=auth-report --timeout 10000"


},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.12.2",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.2",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.6",
    "nodemailer-sendgrid": "^1.0.3",
    "p-limit": "^7.2.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "chai": "^6.2.1",
    "chai-http": "^5.1.2",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "jest": "^29.7.0",
    "mocha": "^11.7.4",
    "nodemon": "^3.1.10",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4"
  }
}